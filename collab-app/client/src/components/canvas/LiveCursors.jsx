// LiveCursors dirender langsung di dalam Stage Konva (canvas/CollabCanvas.jsx)
// File ini placeholder jika diperlukan sebagai overlay HTML di luar canvas

export default function LiveCursors({ cursors }) {
  return null // Cursor ditangani di dalam Konva Layer
}
